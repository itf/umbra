# Meeting — Cycle 10 Planning (brief)

**Context:** Release-ready checkpoint (`f63e2d7`). User is reviewing via SESSION-HANDOFF
/ NEEDS-PLAYTEST; loop continues on the backlog until redirected.

## Decision: wire the proven Steam-SOFA capability into papasangre

Of the backlog (online leaderboard, Steam-SOFA, narrative, seed-share, more content),
**Steam-SOFA** is the best next move: it *completes* something already built + de-risked
this session (the `feat/sofa-hrtf` branch in the sibling `three-steam-audio` clone proved
`iplHRTFCreate` accepts our SADIE SOFA, ~12-line WASM change, upstream-PR-ready). Wiring it
into papasangre means the optional `?engine=steam` path uses OUR measured ears instead of
Steam's generic HRTF — consistent with the trainer/default engine, and the actual payoff of
that whole investigation. The others are net-new scope; this ties off a thread.

**Risk to manage:** it depends on the sibling clone's rebuilt WASM (gitignored build
artifact), so consuming it cleanly is the real work — vendoring vs a local dep link. The
agent must keep the DEFAULT path completely unaffected (Steam stays opt-in, lazy-loaded,
graceful-fallback) and not regress the lean bundle (the cycle-9 audit just verified
three/phonon are lazy-only).

## Action
- Dispatch: wire papasangre's Steam backend to load + pass the SADIE SOFA ArrayBuffer to
  `three-steam-audio`'s new `{ type:'sofa', data }` API, consuming the sibling clone's
  `feat/sofa-hrtf` build. Gate on tsc+unit+e2e+build; the default `?engine=ours` path must
  stay byte-identical and the bundle lean.
- If consuming the local fork is too invasive/fragile to do cleanly, STOP and report —
  don't force a brittle dependency wiring; fall back to the next backlog item.
