# Meeting — Cycle 11 Planning (brief)

**Context:** Checkpoint `5b46467`. Loop continues on backlog until redirected.

## Decision: sandbox SEED-SHARE (finish the sandbox feature)

Of the backlog:
- **Online leaderboard** — needs a backend/server; out of scope for a local PWA. Defer.
- **Narrative/campaign** — large net-new scope; better with explicit user direction. Defer.
- **Sandbox seed-share** — SMALL, self-contained, COMPLETES 8C. The pure helpers already
  exist (`sandboxShareString`, `parseSeed` in sandbox.ts) but the UI only auto-advances a
  hidden seed counter — there's no way to SEE/COPY the current seed or ENTER a specific one.
  Wiring that gives the infinite-content mode real replay/social value (share a great roll).

Best pick: low-risk, finishes a feature rather than opening new scope.

## Action
- Dispatch: surface the current sandbox seed (display + copy), and let the player enter a
  seed (or a mode|difficulty|seed share string via `parseSeed`/`sandboxShareString`) to
  reproduce an exact level. Determinism already guaranteed; this is UI + wiring + a couple
  pure helpers. Gate tsc+unit+e2e+build; accessibility-first (labeled, announced).
